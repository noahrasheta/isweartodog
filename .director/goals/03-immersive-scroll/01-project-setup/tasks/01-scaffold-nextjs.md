# Task: Scaffold Next.js 16 project with TypeScript and Tailwind CSS 4

## What To Do

Initialize a new Next.js 16 project with TypeScript and Tailwind CSS 4. Set up the basic file structure for a single-page app. Configure the project for static generation on Vercel (do NOT add output: 'export'). Add .gitignore, set up the public/ directory structure for artwork (public/art/). Initialize the git repo if not already done.

## Why It Matters

The foundation everything else builds on. Getting the scaffold right means no configuration headaches later.

## Size

**Estimate:** small

Standard project initialization. Well-documented, straightforward.

## Done When

- [ ] Next.js 16 project initialized with TypeScript
- [ ] Tailwind CSS 4 installed and configured
- [ ] @tailwindcss/typography plugin installed
- [ ] Basic app/layout.tsx and app/page.tsx exist
- [ ] public/art/ directory created for artwork
- [ ] .gitignore includes .env*.local and .next/
- [ ] Project runs with next dev without errors

## Needs First

Nothing — this can start right away.
